// GitCode.dev/frontend/app/auth/callback/layout.tsx
import { ReactNode } from 'react';

export default function AuthCallbackLayout({ children }: { children: ReactNode }) {
  return (
      <>{children}</>
  );
}